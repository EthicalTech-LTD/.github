# EthicalTech LTD

EthicalTech is a UK based development studio which aims to build ethical software.

For example:

 - if we built an ad network, it would block any ads which uses "dark patterns" such as misleading text, fake click buttons etc.
